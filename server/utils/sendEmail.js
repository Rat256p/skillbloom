const sendEmail = (to, subject, message) => {
    console.log(`📧 Simulating email to: ${to}`);
    console.log(`Subject: ${subject}`);
    console.log(`Message: ${message}`);
    // You can integrate nodemailer later
  };
  
  export default sendEmail;
  